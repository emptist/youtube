# Dependencies for audio noise reduction applications
# Core requirements for noise reduction
noisereduce>=2.0
librosa>=0.10.0
numpy>=1.21.0
scipy>=1.7.0
soundfile>=0.12.0

# For audio compression and format conversion
pydub>=0.25.1

# System dependencies (not installable via pip)
# ffmpeg is required for all functionality
# Installation instructions:
# - macOS: brew install ffmpeg
# - Windows: Download from https://ffmpeg.org/download.html and add to PATH

# Optional: For creating standalone executables
pyinstaller>=5.0.0